env:
  - DOCKER_PASSWORD=fzy302525 DOCKER_USERNAME=fengzhanyuan
sudo: required
language: python
services:
  - docker
python:
  - "3.6"
cache: pip
before_install:
  - docker info
  - docker ps -a
install:
  - pip install -r requirements.txt
script:
  - pytest -v tests.py
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker version
  - pwd
  - docker build -t lyrewing/skyline-admin .
  - docker push lyrewing/skyline-admin
  - docker images
